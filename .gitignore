# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# Export zip — too large for GitHub (451MB), download from Replit Files panel instead
stillafloat-export.zip
*.export.zip

# compiled output
dist
dist-test
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Generated news pages (written on the box by the news pre-renderer —
# server/src/lib/prerender-news.ts; news.html + es/news.html stay tracked but
# are overwritten in place on the box)
server/public/news/
server/public/es/news/
server/public/news-sitemap.xml

# Replit (legacy — migrated off Replit to Hetzner; keep these out)
.cache/
.local/
.replit
.replitignore
**/.replit-artifact/
server/.env
server/start.sh
server/ecosystem.config.cjs

# cabin-advisor: per-ship intermediate chunks that combine into *-full.json.
# Derived output — regenerate rather than version ~4MB of them.
cabin-advisor/data/cabins/parts/
